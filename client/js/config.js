// Настройки клиента. Подключается тегом <script> раньше остальных скриптов,
// ничего собирать/пересобирать не нужно — просто правишь значения и обновляешь страницу.
const CLIENT_CONFIG = {
  // Как часто (мс) опрашивать сервер за статусом истории, пока вкладка "История" открыта.
  HISTORY_POLL_INTERVAL_MS: 3000,

  // Сколько (мс) после нажатия "Сформировать сейчас" держать заглушку "запускается"
  // и продолжать поллинг, даже если сервер ещё не показал новый запуск. По истечении —
  // заглушка убирается и поллинг останавливается (если к тому моменту нет ничего "running").
  HISTORY_LOADING_WINDOW_MS: 30000,
};
